
    UnNetHack version 5.0.0

Options compiled into this edition:

    auto open doors, blackmarket level, color, data file compression,
    debug mode, dungeon map overview patch, Elbereth,
    experience points on status line,
    insurance files for recovering from crashes, Keystone Kops, log file,
    mail daemon, news file, pickup thrown objects, redo command, rogue level,
    saddles and riding, screen clipping, seduction, shell command, sinks,
    suspend command, terminal info library, tourists, walled mazes,
    save and bones files accepted from version 5.0.0 only,
    basic UnNetHack features.

Supported windowing systems:

    with a default of tty.

